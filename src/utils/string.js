/**
 * String utility functions
 */
module.exports = {
    /**
     * Converts string to integrer
     *
     * @param {String} number
     * @returns {Number}
     */
    toInt: (number) => {
        return parseInt(number, 10);
    }
}
